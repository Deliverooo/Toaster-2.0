#pragma once

#include "logical_device.hpp"

#include "gpu_common.hpp"

namespace toaster::gpu
{
	// class TST_GPU_API CommandBuffer
	// {
	// TST_REGISTER_DEPENDENCY(LogicalDevice, device)
	// public:
	// CommandBuffer(LogicalDevice& p_device, vk::Queue)

	// private:
	// };
}
