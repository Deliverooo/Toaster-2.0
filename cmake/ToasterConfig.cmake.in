@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

list(APPEND CMAKE_PREFIX_PATH "${CMAKE_CURRENT_LIST_DIR}/../")

find_dependency(fmt REQUIRED)
find_dependency(nfd REQUIRED)

list(APPEND CMAKE_PREFIX_PATH "${CMAKE_CURRENT_LIST_DIR}/../../")
find_dependency(EnTT REQUIRED)

find_dependency(yaml-cpp REQUIRED)

find_dependency(Vulkan REQUIRED COMPONENTS glslangValidator shaderc_combined SPIRV-Tools)

find_dependency(assimp REQUIRED)

include("${CMAKE_CURRENT_LIST_DIR}/ToasterTargets.cmake")

check_required_components(Toaster)